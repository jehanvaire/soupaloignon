# # Utiliser l'image de base MariaDB
# FROM mariadb
# # WORKDIR /

# # Mettre à jour les paquets et installer le client MySQL
# # RUN apt-get update && apt-get install -y mysql-client

# # Créer le répertoire de sauvegarde
# RUN mkdir backup